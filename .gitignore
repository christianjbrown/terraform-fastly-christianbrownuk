# Terraform state — may contain secrets; never commit
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Local provider cache / plugins
.terraform/
.terraform.tfstate.lock.info

# Variable files that may hold secrets (commit only *.example)
*.tfvars
*.tfvars.json
!*.tfvars.example

# Plan output
*.tfplan

# Editor
.idea/
.vscode/
.DS_Store
